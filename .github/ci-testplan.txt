# Test plan for .github/workflows/fork-test-runner.yml (fork-only; never merged upstream).
# One row per change under review. Pipe-separated, blank lines and # comments ignored.
#
#   branch | test file | source file | -k expression | suite dir ("-") | base ref (default main)
#
# The base ref is resolved as origin/<base> first, then verbatim.
# PREFER A RAW SHA. A branch-relative rev such as <branch>~2 is unsafe on a
# branch mergify can update: on 2026-09-10 it merged main into an approved PR
# branch, so <branch>~1 became the fix under test and BEFORE passed. Always
# check the 'HEAD = <sha> (<subject>)' line the runner prints for BEFORE.
#
# The runner checks out <branch>'s test file on top of main and runs the -k
# selection (expected red), then checks out the branch and runs the whole file
# (expected green). If a suite dir is given it runs `pytest -m cpu_test <dir>`
# on main and on the branch so unrelated environment failures are visible as
# identical on both sides.

# ACTIVE, PR 54527 -- @claude round 4: an images_kwargs override must not shrink
# the shared size upper bound that the video budget and dummy data also read.
# BEFORE runs against a0c5180d8c (the previous branch head), not main: modality
# scoping does not exist on main at all.
# Verified (run 34091035237): BEFORE ((700, 700), 95) == ((2184, 2184), 24).
# Re-running post-rebase onto 869f78732b (the branch was rebased 2026-09-07 to
# drop 8 unrelated upstream files that a stray commit had swept in).
# Verified (run 34183531905), PR 54527 open:
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_glm4_1v.py | vllm/model_executor/models/glm4_1v.py | images_kwargs_max_pixels_does_not_leak_into_video_budget | tests/models/multimodal/processing/test_glm4_1v.py | fix/modality-scoped-mm-kwargs-vl~2

# Verified (run 34000087312), PR 54527 open -- per-request mm_processor_kwargs:
# # Verified (run 34296534379): BEFORE 5 failed / 3 passed, 4 of them real
# Idefics3 processor failures; AFTER 3 failed (gated repo 401s) / 20 passed.
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_transformers_image.py | vllm/model_executor/models/transformers/multimodal.py | request_mm_processor_kwargs_reach_the_token_count | tests/models/multimodal/processing/test_transformers_image.py | 6e70a6a25c

# Verified, PR 54527 open (widened to 12 models; two model families covered):
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_glm4_1v.py | vllm/model_executor/models/glm4_1v.py | videos_kwargs_max_pixels_does_not_leak | -
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_transformers_image.py | vllm/model_executor/models/transformers/multimodal.py | scoped_images_kwargs_reach_the_token_count | -

# Verified, PR 54539 open:
# fix/truncate-assistant-tokens-mask | tests/entrypoints/scale_out/render/test_render.py | vllm/renderers/params.py | assistant_tokens_mask_follows_truncation | -

# ACTIVE, PR 54548 -- re-verify after the rebase onto main: upstream moved
# _extract_mm_features into token_in_token_out/mm_features.py, so the fix and
# the rebuild helper moved with it.
# Verified (run 34183531905), PR 54548 open:
# fix/render-features-is-embed | tests/entrypoints/scale_out/token_in_token_out/test_mm_serde.py | vllm/entrypoints/scale_out/token_in_token_out/mm_features.py | render_features_preserve_is_embed | tests/entrypoints/scale_out/token_in_token_out

# ACTIVE, PR 54548 -- CodeRabbit round 1: a client-supplied is_embed shorter than
# the span must be rejected. BEFORE runs against the previous branch head, where
# is_embed exists but is unvalidated.
# Verified (run 34183531905), PR 54548 open:
# fix/render-features-is-embed | tests/entrypoints/scale_out/token_in_token_out/test_mm_serde.py | vllm/entrypoints/scale_out/token_in_token_out/protocol.py | placeholder_rejects_a_mask_shorter_than_the_span | - | fix/render-features-is-embed~1

# Verified and reported upstream (PR 54548):
# fix/render-features-is-embed | tests/entrypoints/scale_out/token_in_token_out/test_mm_serde.py | vllm/entrypoints/scale_out/token_in_token_out/protocol.py | render_features_preserve_is_embed | tests/entrypoints/scale_out/token_in_token_out

# Verified and reported upstream (PR 54509):
# fix/truncate-prompt-is-token-ids | tests/renderers/test_chat_utils_prompt_embeds.py | vllm/renderers/params.py | truncation_keeps_the_mixed_mask | tests/renderers

# Already verified and reported upstream:
# fix/truncate-token-offsets | tests/entrypoints/scale_out/render/test_render.py | vllm/renderers/params.py | truncated_token_offsets | -
# fix/pad-after-truncate | tests/entrypoints/pooling/scoring/test_io_processor_unit.py | vllm/entrypoints/pooling/scoring/io_processor.py | token_type_ids_stay_aligned_with_a_truncated_padded_prompt | - | fix/pad-after-truncate~1
# fix/pad-after-truncate | tests/renderers/test_completions.py | vllm/renderers/params.py | padding_with_left_truncation_keeps_the_prompt or padding_without_truncation_is_unchanged | -
# fix/pad-after-truncate | tests/entrypoints/pooling/scoring/test_io_processor_unit.py | vllm/entrypoints/pooling/scoring/io_processor.py | token_type_ids_stay_aligned_with_a_truncated_padded_prompt | -
# fix/video-base64-validate | tests/multimodal/media/test_video.py | vllm/multimodal/media/video.py | load_base64_rejects_malformed | tests/multimodal/media
# fix/mm-hash-framing | tests/multimodal/test_hasher.py | vllm/multimodal/hasher.py | collision_kwargs_key_value_boundary or collision_nested_vs_flattened_key or collision_sequence_vs_mapping or collision_none_vs_empty or collision_empty_container_vs_omitted | tests/multimodal
# fix/encoder-cache-repeated-item | tests/v1/core/test_encoder_cache_manager.py | vllm/v1/core/encoder_cache_manager.py | duplicate_mm_hash_stays_referenced_until_last_free or duplicate_mm_hash_is_not_evicted_before_last_use | tests/v1/core

# ACTIVE, PR 54527 -- DarkLight1337 asked whether the `call_mm_kwargs` hunk in
# LegacyMultiModalProcessor.apply() is only a variable rename. BEFORE runs
# against probe/call-mm-kwargs-shadow, which is this branch's head with ONLY
# that hunk reverted (main's rebind of the parameter name restored) -- so a red
# BEFORE here isolates exactly what the rename buys.
# Verified (run 34296534379): BEFORE 5 failed / 3 passed.
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_transformers_image.py | vllm/model_executor/models/transformers/multimodal.py | tokens_structuring_an_image_are_masked_not_dropped or nested_image_fields_split_per_image or spliced_prompt_matches_hf_expansion or repeated_image_hits_the_processor_cache | - | probe/call-mm-kwargs-shadow

# RETIRED 2026-09-11 (run 34521803748) -- DarkLight1337 asked for the test to be
# removed, since pre-commit mypy reports the same [override] error. Kept for the
# record. Originally: upstream pre-commit mypy caught that Glm5NextProcessingInfo
# overrides _get_image_max_pixels without the new modality parameter while
# inheriting get_image_size_with_most_features, which now passes modality=None.
# BEFORE runs against the previous branch head (40d876a492), where the base has
# the parameter and the subclass does not -- so BEFORE is the TypeError itself.
# fix/modality-scoped-mm-kwargs-vl | tests/models/multimodal/processing/test_glm4_1v.py | vllm/models/glm5next/nvidia/multimodal.py | subclasses_keep_the_modality_parameter | tests/models/multimodal/processing/test_glm4_1v.py | 40d876a49257897fc5dcea6a2632f4d19727825c

# ACTIVE, PR 54323 -- 14-day bump: fresh verification against current main after
# rebasing and correcting the test docstring (it still claimed a 500, which the
# PR body had already retracted). Sentinel is the assert_never_reached loader:
# BEFORE proves the corrupted stream reaches the decoder, not merely that some
# exception is raised.
# Verified (run 34710923235): BEFORE AssertionError 'video decoder reached with
# undecodable base64'; AFTER 39 passed; suite 158 vs 164 passed, 0 failed both sides.
# fix/video-base64-validate | tests/multimodal/media/test_video.py | vllm/multimodal/media/video.py | test_load_base64_rejects_malformed | tests/multimodal/media | main
